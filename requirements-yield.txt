# Optional dependencies for the federated tomato yield prediction model.
# Required only when using federated/yield_predictor.py.
#
# Install:
#   pip install -r requirements-yield.txt

torch>=2.0
scikit-learn>=1.3
joblib>=1.3
